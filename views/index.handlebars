<h1>Burgers!</h1>

<h2>Burgers that are tasty!</h2>

<ul>
  {{#each burgers}}
    {{#unless tasty}}
      {{> burgers/burger-block tasty=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are not tasty?</h2>

<ul>
  {{#each burgers}}
    {{#if tasty}}
      {{> burgers/burger-block tasty=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Tasty?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Tasty!<br>
    <input type="radio" name="sleepy" value="0"> Gross
  </div>

  <button type="submit">Add Burger</button>
</form>
